<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_product"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:background="@color/colorPrimaryDark2"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="utng.edu.mx.employee.EmployeeActivity">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/et_bith_date"
        android:inputType="text"
        android:hint="@string/birth_date"
        />
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="text"
        android:hint="@string/first_name"
        android:layout_below="@+id/et_bith_date"
        android:id="@+id/et_first_name"/>
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="text"
        android:hint="@string/last_name"
        android:id="@+id/et_last_name"
        android:layout_below="@+id/et_first_name"/>
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/save_employee"
        android:background="@color/colorPrimary2"
        android:layout_below="@+id/et_last_name"
        android:id="@+id/bt_save_product"/>

    <android.support.v7.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/bt_save_product"
        android:id="@+id/rv_product_list"
        >

    </android.support.v7.widget.RecyclerView>


</RelativeLayout>
